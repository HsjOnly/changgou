eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka
spring:
  application:
    name: eureka_server
server:
  port: 7001